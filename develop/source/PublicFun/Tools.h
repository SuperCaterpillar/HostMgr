#ifndef TOOLS_H
#define TOOLS_H
#include "Publicdefine.h"

namespace Tools {

    vector<string> SplitString(const string & src, const string &pattern);
    bool is_number(const std::string& str);
}

#endif // TOOLS_H
